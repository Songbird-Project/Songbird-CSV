# String

> [!NOTE]
> The output varies due to the random read order of maps in Go. If a consistent
> order is needed then this can be achieved via sorting.

This example is the same as ["Package List"](../package-list/) but it uses a
string rather than an input file.
